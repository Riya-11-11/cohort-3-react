Context API

createContext() → Context create karta hai.

Provider → Data provide karta hai.

useContext() → Data consume karta hai.

Props → Parent → Child

Context → Direct Access

Prop Drilling → Same props multiple levels tak pass karna.

Provider ke andar wale components hi Context access kar sakte hain.